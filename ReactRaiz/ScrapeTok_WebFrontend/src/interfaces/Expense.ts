export interface BackendExpenseDetailItem {
  id: number;
  date: string;
  category: { id: number; name: string };
  amount: number;
}

export interface BackendExpenseSummaryItem {
  id: number;
  expenseCategory: { id: number; name: string };
  year: number;
  month: number;
  amount: number;
}
export interface BackendExpenseSummaryItem {
  id: number;
  expenseCategory: { id: number; name: string };
  year: number;
  month: number;
  amount: number;
}
